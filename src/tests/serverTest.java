package tests;

//import static org.junit.Assert.*;

import java.util.ArrayList;

//import org.junit.Test;

import client.ClientNetwork;
import player.Card;

// TODO: Auto-generated Javadoc
/**
 * The Class serverTest.
 */
public class serverTest {

	/**
	 * Test.
	 */
	//@Test
	public void test() {
/*		ClientNetwork.connect();
		System.out.println(ClientNetwork.createAccountOnServer("Jaboby2", "password".toCharArray()));
		System.out.println(ClientNetwork.loginToServer("Jaboby2", "password".toCharArray()));
		System.out.println(ClientNetwork.createAccountOnServer("Jaboby", "password".toCharArray()));
		System.out.println(ClientNetwork.loginToServer("Jaboby", "password".toCharArray()));
		System.out.println(ClientNetwork.createAccountOnServer("Jabob", "password".toCharArray()));
		//String userid = ClientNetwork.loginToServer("Jabob", "password".toCharArray());
		ArrayList<String> users = ClientNetwork.getUsernames();
		System.out.println("Users: ");
		for(String s : users) {
			System.out.print(s + ", ");
		}
		System.out.println();
		
		ArrayList<Card> cardLibrary = ClientNetwork.getCardLibrary();
		System.out.println(cardLibrary.size());
		for(Card c : cardLibrary) {
			System.out.println(c);
		}
		System.out.println(ClientNetwork.getPlayerCurrency(userid));
		ClientNetwork.setPlayerCurrency(userid, 200000);
		ClientNetwork.getPlayerCurrency(userid);
		ClientNetwork.getCardIDByRarityAddToUser(userid,"Common");
		//ClientNetwork.createUnstartedGame(userID);
		*/
		
	}

}
